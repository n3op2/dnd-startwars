import { addElement, updateElement, removeElement} from './ArrayOperations';
import { removeKey, addKey, updateKeyVal } from './ObjectOperations';
import { updateGameState, updatePanelState, reset, updateLukeFound } from './BasicActions';

export default {
  addKey,
  updateKeyVal, 
  updateLukeFound,
  reset,
  removeKey,
  addElement,
  removeElement, 
  updateGameState,
  updatePanelState,
  updateElement
}
